# Backlog

Open follow-up items for `xian-governance-web`:

- add multi-network environment parsing once public network manifests exist
- decide whether governance history should remain direct-node only or move to a
  BDS/indexer-backed timeline
- add operator deployment examples for the accepted hosting and reverse-proxy
  model
- keep proposal, validator, and state-patch payload rendering defensive as the
  governance contracts evolve

Use `docs/implementation-proposal.md` for broader background. Keep this file
limited to repo-local follow-ups.
